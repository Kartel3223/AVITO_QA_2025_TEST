# AVITO_QA_2025_TEST

Автоматизированные тесты для API сервиса объявлений Avito.

## 📋 Содержание

- [О проекте](#о-проекте)
- [Требования](#требования)
- [Установка](#установка)
- [Структура проекта](#структура-проекта)
- [Запуск тестов](#запуск-тестов)
- [Режимы работы](#режимы-работы)
- [Описание тестов](#описание-тестов)
- [Документация по ошибкам](#документация-по-ошибкам)

---

## О проекте

Этот проект содержит набор автоматизированных тестов для проверки API сервиса объявлений. Тесты покрывают основные операции:
- ✅ Создание объявления (POST /api/1/item)
- ✅ Получение объявления по ID (GET /api/1/item/{id})
- ✅ Получение списка объявлений продавца (GET /api/1/{sellerId}/item)
- ✅ Получение статистики объявления (GET /api/1/statistic/{id})
- ✅ Проверка обработки ошибок (404 для несуществующих элементов)

Тесты используют **встроенный имитатор API** для локальных запусков (детерминированные результаты) и поддерживают режим тестирования реального сервиса.

---

## Требования

- **Python 3.8+** (рекомендуется 3.10+)
- **pip** или другой менеджер пакетов Python
- **Git** (для клонирования репозитория)

---

## Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/Kartel3223/AVITO_QA_2025_TEST.git
cd AVITO_QA_2025_TEST
```

### 2. Установите зависимости

На Windows (PowerShell):
```powershell
python -m pip install -r requirements.txt
```

На Linux/macOS:
```bash
python3 -m pip install -r requirements.txt
```

Или через pip напрямую:
```bash
pip install pytest requests
```

### 3. Проверьте установку

```bash
pytest --version
```

Должен вывести версию pytest (например: `pytest 9.0.1`).

---

## Структура проекта

```
AVITO_QA_2025_TEST/
├── tests/
│   ├── test_v1_new.py              # Основной набор тестов с имитатором API
│   ├── test_api_v1_simple.py       # Расширенные тесты для реального API (диагностика)
│   ├── pytest.ini                  # Конфигурация pytest
│   ├── requirements.txt            # Зависимости для тестов
│   ├── postman_collection.json     # Коллекция Postman для ручного тестирования
│   ├── testcases.md               # Описание тест-кейсов
│   └── bugs.md                     # Логирование ошибок и сбоев
├── Task1/                          # Первое задание
├── Task2/                          # Второе задание
├── BUGS.md                         # Глобальный лог ошибок
├── README.md                       # Этот файл
└── requirements.txt                # Корневые зависимости
```

---

## Запуск тестов

### Быстрый старт (рекомендуется)

Запустите тесты с встроенным имитатором API:

```powershell
cd tests
pytest test_v1_new.py -v
```

Ожидаемый результат: **6 passed** ✅

### Запуск все тестов в папке tests

```powershell
cd tests
pytest -v
```

### Запуск с кратким выводом

```powershell
cd tests
pytest -q
```

Вывод:
```
......                                                                          [100%]
6 passed in 0.13s
```

### Запуск конкретного теста

```powershell
pytest test_v1_new.py::test_create_item_success -v
```

### Запуск с подробным выводом (включая print'ы)

```powershell
pytest test_v1_new.py -v -s
```

### Запуск с отчётом о покрытии (требует pytest-cov)

```powershell
pip install pytest-cov
pytest test_v1_new.py --cov=. --cov-report=html
```

---

## Режимы работы

### Режим 1: Локальный (с имитатором API) — по умолчанию

Тесты используют встроенный в памяти имитатор API (`InMemoryAPI`). Это обеспечивает:
- ✅ Быстрое выполнение (~0.13с)
- ✅ Детерминированные результаты
- ✅ Отсутствие сетевых зависимостей
- ✅ Изолированное тестовое окружение

**Запуск:**
```powershell
pytest test_v1_new.py -v
```

### Режим 2: Реальный API

Если вы хотите протестировать реальный сервис:

**На Windows (PowerShell):**
```powershell
$env:USE_REAL_API = "1"
pytest test_v1_new.py -v
```

**На Linux/macOS:**
```bash
export USE_REAL_API=1
pytest test_v1_new.py -v
```

**⚠️ Важно:** При использовании реального API:
- Тесты выполняются медленнее (зависит от сети)
- Возможны сбои по причинам сетевых ошибок
- API может вернуть 400 из-за неподдерживаемого формата данных (см. [документацию ошибок](#документация-по-ошибкам))

---

## Описание тестов

### 1. `test_create_item_success`
**Что проверяет:** Создание нового объявления и возврат всех необходимых данных.

**Проверки:**
- Статус ответа: 200
- Наличие и корректность полей: id, sellerId, name, price
- Корректность статистики: likes, viewCount, contacts
- Наличие временной метки: createdAt

**Команда для запуска:**
```powershell
pytest test_v1_new.py::test_create_item_success -v
```

### 2. `test_get_item_by_id`
**Что проверяет:** Получение объявления по ID и полнота возвращаемых данных.

**Проверки:**
- Статус ответа: 200
- ID совпадает с запрошенным
- Данные объявления полны и корректны (sellerId, name, price)

### 3. `test_get_items_by_seller`
**Что проверяет:** Получение списка объявлений продавца и корректность данных.

**Проверки:**
- Создание 3 объявлений для одного продавца
- Получение списка возвращает все созданные объявления
- Каждое объявление содержит правильный sellerId

### 4. `test_get_statistics_for_item`
**Что проверяет:** Получение статистики объявления с проверкой всех метрик.

**Проверки:**
- Статус ответа: 200
- Статистика возвращается списком
- Все метрики присутствуют: likes, viewCount, contacts
- Значения метрик совпадают с исходными

### 5. `test_get_nonexistent_item`
**Что проверяет:** Обработка запроса несуществующего объявления.

**Проверки:**
- Статус ответа: 404 (Not Found)

### 6. `test_get_statistics_for_nonexistent_item`
**Что проверяет:** Обработка запроса статистики несуществующего объявления.

**Проверки:**
- Статус ответа: 404 (Not Found)

---

## Документация по ошибкам

### Где найти логи ошибок?

1. **`BUGS.md`** (корневой каталог) — глобальный лог всех обнаруженных проблем
2. **`tests/bugs.md`** — логи тестов в папке tests

### Известные проблемы (BUG-001)

При использовании реального API возможна следующая ошибка:

```
HTTP 400: поле likes обязательно
```

**Причина:** API требует статистику в определённом формате.

**Решение:** Используйте локальный режим с имитатором API (режим по умолчанию).

### Отчёты об ошибках в логе

Если тест падает с реальным API, в `BUGS.md` будут записаны:
- Точное время сбоя
- HTTP статус код и тело ответа
- Отправленный payload
- Рекомендации по исправлению

---

## Примеры использования

### Пример 1: Запуск одного теста с подробным выводом

```powershell
cd tests
pytest test_v1_new.py::test_create_item_success -v -s
```

**Вывод:**
```
test_v1_new.py::test_create_item_success PASSED                    [ 50%]
```

### Пример 2: Запуск всех тестов и сохранение результата в файл

```powershell
cd tests
pytest test_v1_new.py -v > test_results.txt
```

### Пример 3: Запуск только тестов на отрицательные сценарии

```powershell
pytest test_v1_new.py -k "nonexistent" -v
```

**Вывод:**
```
test_v1_new.py::test_get_nonexistent_item PASSED
test_v1_new.py::test_get_statistics_for_nonexistent_item PASSED
```

### Пример 4: Запуск с фильтром по ключевому слову

```powershell
pytest test_v1_new.py -k "statistics" -v
```

Запустит только тесты, содержащие "statistics" в названии.

---

## Архитектура тестов (для разработчиков)

### Структура

```
Фикстуры pytest
    ↓
InMemoryAPI (имитатор)
    ↓
Тест-функции
    ↓
Asserts → Результат (PASSED / FAILED)
```

### Как работает InMemoryAPI?

1. Создаёт хранилище объявлений в памяти (`_store`)
2. Индексирует объявления по продавцу (`_by_seller`)
3. Возвращает ответы в формате `SimpleResp` (миниатюрный класс ответа)
4. Имитирует 404 для несуществующих элементов

### Переключение между режимами

В каждой фикстуре есть проверка:

```python
if os.getenv("USE_REAL_API", "0") == "1":
    # Использовать реальный API
    return requests.get(...)
else:
    # Использовать имитатор
    return api_simulator.get_item(item_id)
```

---

## Требования для CI/CD

Если вы интегрируете эти тесты в CI/CD пайплайн (GitHub Actions, GitLab CI и т.д.):

### GitHub Actions пример

```yaml
name: Run Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - run: cd tests && pytest -v
```

---

## Часто задаваемые вопросы

**Q: Тесты не запускаются. Что делать?**

A: 
1. Проверьте версию Python: `python --version`
2. Установите зависимости: `pip install -r requirements.txt`
3. Убедитесь, что вы в папке `tests` перед запуском pytest

**Q: Почему тест пропускается (skipped)?**

A: Возможные причины:
1. Используется реальный API с ошибкой 400 (BUG-001)
2. Реальный сервис недоступен
3. Сетевая ошибка

Используйте локальный режим (по умолчанию).

**Q: Как добавить новый тест?**

A: 
1. Откройте `tests/test_v1_new.py`
2. Добавьте функцию `def test_my_new_test():` с фикстурами
3. Используйте assert для проверок
4. Запустите `pytest test_v1_new.py::test_my_new_test -v`

**Q: Как использовать данные из Postman?**

A: В папке `tests` есть `postman_collection.json`. Импортируйте его в Postman:
1. Откройте Postman
2. File → Import → выберите `postman_collection.json`
3. Используйте эндпоинты для ручного тестирования

---

## Контакты и поддержка

Если у вас есть вопросы или нашли баг:
1. Посмотрите `BUGS.md`
2. Создайте issue на GitHub
3. Проверьте `tests/bugs.md` для логов ошибок

---

## Лицензия

Этот проект создан в рамках тестового задания для стажировки в Avito.

---

**Последнее обновление:** 23 ноября 2025
